import { Dish } from '../../api/types/DishTypes';

export type DishComponentProps = {
  dish: Dish;
  //   token: string | null;
  //   handleDeleteClick: (id: string) => void;
  //   handleMainChecked: (id: string, checked: boolean) => void;
  //   handleDishUpdated: (newDish: Dish) => void;
  //   dishError?: string;
  //   disabled?: boolean;
};
